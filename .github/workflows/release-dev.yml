name: Debug Dockerfile

on:
  workflow_dispatch:

jobs:
  debug-only:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Debug - Verificar archivos
        run: |
          echo "=== BUSCANDO DOCKERFILES EN TODO EL REPO ==="
          find . -name "Dockerfile*" -type f
          echo "=== LISTANDO CARPETA RAIZ ==="
          ls -la
          echo "=== LISTANDO ./Back/ ==="
          ls -la ./Back/ || echo "No existe"
          echo "=== LISTANDO ./Back/ms_expense/ ==="
          ls -la ./Back/ms_expense/ || echo "No existe"
          echo "=== LISTANDO ./Back/ms_expense/ms_expense/ ==="
          ls -la ./Back/ms_expense/ms_expense/ || echo "No existe"
